app:
  name: "mock-demo"
  display_name: "Mock功能演示"

# Mock配置 - 三级配置：全局、分组、服务
mock:
  global:
    enabled: false

  groups:
    "用户管理":
      enabled: true

  services:
    "get_order":
      enabled: true